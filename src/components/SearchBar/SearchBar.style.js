import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    backgroundColor: '#eceff1',
    borderRadius: 10,
    marginBottom: 10,
    marginHorizontal: 10,
    padding: 15,
  },
});
